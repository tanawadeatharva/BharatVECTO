<UserControl x:Class="VECTO3.Views.JoblistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:VECTO3.ViewModel"
             xmlns:helper="clr-namespace:VECTO3.Helper"
             xmlns:impl="clr-namespace:VECTO3.ViewModel.Impl"
             xmlns:interfaces="clr-namespace:VECTO3.ViewModel.Interfaces"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <d:JoblistView.DataContext>
        <x:Type Type="interfaces:IJoblistViewModel"/>
    </d:JoblistView.DataContext>
    <UserControl.Resources>
        <helper:JobEntrySelectedConverter x:Key="JobEntrySelectedConverter" />
        
    </UserControl.Resources>
    <Grid Margin="0,0,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>
        <ListView x:Name="Joblisting" Height="156" Margin="10,10,10,0" VerticalAlignment="Top" Grid.Column="0"
            ItemsSource="{Binding Jobs}" MouseDoubleClick="Joblisting_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="30">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate DataType="impl:JobEntry">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding Path=Selected}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Filepath" Width="auto" DisplayMemberBinding="{Binding Filename}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Column="1">
            <Button Margin="0,20,0,0" Command="{Binding EditJob}" CommandParameter="{Binding SelectedItems, ElementName=Joblisting}">Edit</Button>
            <Button Margin="0,15,0,0" Command="{Binding AddJob}">Add</Button>
            <Button Margin="0,5,0,0" Command="{Binding RemoveJob}" CommandParameter="{Binding SelectedItems, ElementName=Joblisting}">Remove</Button>
            <Button Margin="0,5,0,0" Command="{Binding MoveJobUp}">Up</Button>
            <Button Margin="0,5,0,0" Command="{Binding MoveJobDown}">Down</Button>
            <Button Margin="0,20,0,0" Command="{Binding StartSimulation}">Start</Button>
        </StackPanel>
    </Grid>
</UserControl>
